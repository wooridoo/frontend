# WOORIDO Active Navigation

- Active architecture: Go API + PostgreSQL + worker
- Active public API owner: one backend only
- Active documentation source: `docs/00_CANONICAL/*` when present
- Fallback bundled references: this folder
- Legacy materials: `.woorido/references/legacy/*` only during migration
