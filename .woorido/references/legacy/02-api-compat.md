# Legacy API Compatibility

- Legacy docs split APIs across Spring and Django
- Active docs use one public backend only
- Legacy `/search`, `/analytics`, `/recommendations` routes are not active defaults
